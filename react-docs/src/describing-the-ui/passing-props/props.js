// props serve the same role as arguments to function
// React Component accepts one single argument, props object

// passing JSX as children, using children prop
// props are immutable, but can change over time
